/.idea/*
src/.vscode/*
/.ionide/*


# Autogenerated LaTeX-related files
src/*.aux # An auxiliary file that saves information for the creation of ToC, references, indices, bibliographies and other things like that. It is reread in the next compiling to create the ToC, references etc.
src/*.toc # An auxiliary file that stores the Table of Contents, read in on subsequent runs to create the actual ToC.
src/*.log # Stors all messages of the compilation, like errors and warnings. It’s used by most Editors / LaTeX IDEs to show the errors in you document.
src/*.nav # contains the information needed for the navigation bar hyperlinks.
src/*.vrb # will contain the verbatim material when fragile (containing verbatim material) frames.
src/*.snm # will contain information that will be used by \pgfimage to include actual slides when using the beamerarticle package.
src/*.tuc # Data stored for multiple passes, ToC, etc.
# (La)TeX:
# Lists:
src/*.lof # An auxiliary file that stores the List of Figures, read in on subsequent runs to create the actual LoF.
src/*.lot # An auxiliary file that stores the List of Tables, read in on subsequent runs to create the actual LoT.
# Index:
src/*.idx
src/*.ind
src/*.ilg
# Glossary:
src/*.glo
*.gls
*.glg
# Bib(La)TeX/Biber:
*.bbl # bibliography environment, generated by BibTeX.
*.blg # bibtex and biber log file.
*.bcf # biblatex control file.
*.run.xml # biber XML file.
src/*-blx.bib
# hyperref:
*.out # contains information about the sectional units that will be used to write the outlines.
src/prace.pdf
src/prace.synctex.gz
src/*.log
src/*.bbl
src/*.blg
src/*.fdb_latexmk
src/*.aux
src/*.xmpi
src/*.lof
src/*.lot
src/*.out
src/*.fls
src/*.toc
src/*.run.xml
src/appendices/*.aux
src/chapters/*.aux
src/prace.lol
